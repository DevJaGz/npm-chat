import { RenderSvgDirective } from './render-svg.directive';

describe('RenderSvgDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderSvgDirective();
    expect(directive).toBeTruthy();
  });
});
